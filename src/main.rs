#![allow(unused_imports)]
use itertools::Itertools;
use proconio::{marker::*, *};
use rand::prelude::*;
use std::{cmp::*, collections::*, *};
use superslice::*;

#[fastout]
fn main() {
    input! {
        
    }
}
